/**
 * Curated event → lineage focus metadata lives in `src/data/eventLineageMap.json`.
 * Use `getEventLineageMapEntry`, `getMergedEventFocusIds`, and `getEventPrimaryPathIds` from `src/lib/lineageMatch.js`.
 */
import eventLineageMap from '../../data/eventLineageMap.json';

export default eventLineageMap;
