# Shared environment defaults — safe for all environments
# For local development, copy .env.example to .env.local and fill in your values
# VITE_API_BASE is intentionally left empty so the app uses relative /api paths on Vercel
VITE_API_BASE=
